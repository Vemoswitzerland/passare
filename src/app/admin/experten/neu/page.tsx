import { ExpertenForm } from '../ExpertenForm';

export const metadata = { title: 'Neuer Experte · Admin — passare', robots: { index: false, follow: false } };

export default function NeuerExpertePage() {
  return <ExpertenForm />;
}
